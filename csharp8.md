# C# 8 command

```bash
# Replace YourProfile with the name of the profile defined in your launchSettings.json.
dotnet run --launch-profile YourProfile

# Example
dotnet run --launch-profile https

# Run a .NET application in a specific directory,
dotnet run --project src/ata-pnp-crmre-api/ata-pnp-crmre-api.csproj
```
